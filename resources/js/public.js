import Alpine from 'alpinejs';

/*
| Public-site JavaScript entry.
| Just Alpine for light interactivity (mobile nav, FAQ accordions, smooth UI).
| Deliberately minimal — no Vue/Inertia on public pages for fast loads.
*/

window.Alpine = Alpine;
Alpine.start();
